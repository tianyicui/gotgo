set -ev

make bin/mkmake

# Let's make sure the generated makefile will actually work...
./bin/mkmake > Makefile

make

./bin/gotimports tests/example.go

test -f 'tests/demo/slice.gotgo'
test -f 'tests/demo/slice(list.List).go'
test -f 'tests/test(string).go'

make test

./tests/example

